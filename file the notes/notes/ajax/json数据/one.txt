1.json简单来说就是一个对象 	json--->{}为了数据传输

2.json的方法
JSON.stringify(obj)返回一个字符串--->转化成字符串格式的JSON (前端传给后端的数据)
JSON.parse(obj)返回一个对象----->转化成一个对象我们就可以用他们的属性和方法了 (后端传给前端的数据)

3.domTree 深度优先原则，在广度扩张。(纵线的)  ，如识别完heard在识别body

4.dom树解析和加载时异步的。

5.tranTree = domTree + cssTree

5.js异步加载的方法(tranTree绘制完，等全部绘制完成后在执行里面的代码)
defer="defer"(凡是属性名和属性值一样的话可以只写一个defer) 在js文件夹src=""后面写上这方法 
只有IE可以使用  全部dom文档解析完成后执行
aysnc(和defer用法一样但这个只能加载外部的js文件)非ie可以使用
